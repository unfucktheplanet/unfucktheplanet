<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% unless signed_in? %>
	<%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook), target: "_blank" %>
<% else %>
	<p>Signed in already</p>
<% end %>
<br/><br/>
<div>
	<button onClick="startTheFuck()" >Start</button>
</div>
<div id="container">

</div>